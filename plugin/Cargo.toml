[package]
name = "plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib"]

[dependencies]
sfml = { version = "*", default-features = false }
state = { path = "../state" }

[lints]
workspace = true
