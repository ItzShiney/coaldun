[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "*"
# image = { version = "*", features = ["png"], default-features = false }
libloader = "*"
sfml = "*"
signals = { path = "../signals" }
